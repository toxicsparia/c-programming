#include <stdio.h>

int main() {
    float radius;
    printf("Enter the radius here: \n");
    scanf("%f", &radius);
    printf("The circumference of the circle is: %f \n", 2*3.14*radius);
    return 0;
}